/**
 * Re-export du client Prisma 7 généré.
 * Utilisez ce module au lieu de '../../app/database/prisma' pour que le runtime résolve le client généré.
 */
export * from '../../../prisma/generated/prisma/client';
export type FilterGroup<T> = T & { AND: T[] };
